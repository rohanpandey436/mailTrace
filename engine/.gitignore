# Build artefacts of the optional mailtrace_engine extension.
# Kept here rather than in the repo-root .gitignore so the engine directory is
# self-contained: delete it and nothing else changes.
build/
dist/
*.egg-info/
*.so
*.pyd
*.dll
*.dylib
*.o
*.obj
*.pdb
*.ilk
*.exp
*.lib
CMakeCache.txt
CMakeFiles/
compile_commands.json
